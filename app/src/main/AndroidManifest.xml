<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apollo.consulta_correios">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConsultaCorreios">
        <activity
            android:name=".ActivityAbout"
            android:exported="false" />
        <activity
            android:name=".ActivityHistory"
            android:exported="false" />
        <activity
            android:name=".ActivityLoadPackage"
            android:exported="false" />
        <activity
            android:name=".ActivityInicio"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityShowPackageResult"
            android:exported="true" />
    </application>

</manifest>